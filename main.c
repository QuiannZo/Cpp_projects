#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

#include "refuges.h"

int main(void) {
    int rows = 0, cols = 0;
    if((scanf("%d %d", &rows, &cols) == 2)){
        fprintf(stderr, "Could not read data.");
    }
    
}